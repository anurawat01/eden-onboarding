import { Typography } from "@mui/material";

export default function Heading(){
    return(
        <>
        <Typography variant="h4">Welcome! First things first...</Typography>
        <Typography>You can always change them later</Typography>
        </>
    );

}