import { Box, Typography } from "@mui/material";

export default function heading(){
    return(
        <Box sx={{textAlign: "center"}}>
        <Typography variant="h4">How are you planning to use Eden?</Typography>
        <Typography>We will streamline your setup experience accordingly.</Typography>
        </Box>
    );
}