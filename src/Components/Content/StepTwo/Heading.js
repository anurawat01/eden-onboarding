import { Typography } from "@mui/material";

export default function Heading(){
    return(
        <>
        <Typography variant="h4">Let's set up a home for all your work</Typography>
        <Typography>You can always create another workspace later.</Typography>
        </>
    );

}